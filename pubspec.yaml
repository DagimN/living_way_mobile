name: living_way
description: "An app for the Living Way church to be able to sync their data with their programs, events, seminars and have a personal interaction with the bible which includes vast resources and tools at their hands."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.31.1+117

environment:
  sdk: ">=3.3.1 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_native_splash: ^2.4.0
  flutter_launcher_icons: ^0.13.1
  flutter_svg: ^2.0.10+1
  provider: ^6.1.2
  lottie: ^3.1.2
  cached_network_image: ^3.3.1
  uuid: ^4.4.0
  mini_music_visualizer: ^1.1.4
  audioplayers: ^6.0.0
  url_launcher: ^6.3.0
  flutter_polls: ^0.1.6
  intl: ^0.20.2
  flutter_staggered_grid_view: ^0.7.0
  photo_view: ^0.15.0
  pretty_qr_code: ^3.3.0
  package_info_plus: ^8.0.2
  font_awesome_flutter: ^10.7.0
  google_sign_in: ^6.2.1
  dio: ^5.7.0
  webview_flutter: ^4.8.0
  encrypt: ^5.0.3
  flutter_dotenv: ^5.2.1
  hl_image_picker: ^1.2.16
  permission_handler: ^11.3.1
  path_provider: ^2.1.4
  firebase_crashlytics: ^4.1.0
  firebase_analytics: ^11.6.0
  firebase_core:
  video_player: ^2.9.5
  shimmer: ^3.0.0
  functional_status_codes: ^3.2.0
  shared_preferences: ^2.5.5
  gal: ^2.3.2
  share_plus: ^12.0.2
  http: ^1.6.0
  path: ^1.9.1
  pdfrx: ^2.2.24
  image: ^4.8.0
  disk_space_2: ^1.0.12
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0
  firebase_messaging: ^15.2.10
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  add_2_calendar: ^3.0.1
  youtube_player_flutter: ^9.1.3
  media_store_plus: ^0.1.3
  easy_localization: ^3.0.8
  flutter_secure_storage: ^10.3.1
  crypto: ^3.0.7
  shorebird_code_push: ^2.0.6
  flutter_hooks: ^0.20.5
  percent_indicator: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  logger: ^2.4.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  analyzer: ^6.4.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter_native_splash:
  image: assets/splash/logo.gif
  android: true
  ios: true
  color: "#000000"

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/svg/
    - assets/images/
    - assets/icons/
    - assets/animations/
    - assets/data/
    - assets/translations/
    - .env
    - shorebird.yaml

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    #English
    - family: Futura
      fonts:
        - asset: lib/core/fonts/English/Futura-Round.ttf
    - family: Georgia
      fonts:
        - asset: lib/core/fonts/English/Georgia.ttf
    - family: Helvetica
      fonts:
        - asset: lib/core/fonts/English/Helvetica-Bold-Font.ttf
    - family: OpenSans
      fonts:
        - asset: lib/core/fonts/English/OpenSans-Regular.ttf
    - family: Quicksand
      fonts:
        - asset: lib/core/fonts/English/Quicksand-Font/Quicksand_Bold_Oblique.otf
          weight: 700
          style: italic
        - asset: lib/core/fonts/English/Quicksand-Font/Quicksand_Bold.otf
          weight: 700
        - asset: lib/core/fonts/English/Quicksand-Font/Quicksand_Book_Oblique.otf
          style: italic
        - asset: lib/core/fonts/English/Quicksand-Font/Quicksand_Book.otf
        - asset: lib/core/fonts/English/Quicksand-Font/Quicksand_Light.otf
          weight: 400
        - asset: lib/core/fonts/English/Quicksand-Font/Quicksand_Light_Oblique.otf
          weight: 400
          style: italic
    - family: RobotoSlab
      fonts:
        - asset: lib/core/fonts/English/Roboto_Slab/RobotoSlab-Bold.ttf
          weight: 700
        - asset: lib/core/fonts/English/Roboto_Slab/RobotoSlab-Light.ttf
          weight: 400
        - asset: lib/core/fonts/English/Roboto_Slab/RobotoSlab-Regular.ttf
        - asset: lib/core/fonts/English/Roboto_Slab/RobotoSlab-Thin.ttf
          weight: 200

  #Amharic
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
